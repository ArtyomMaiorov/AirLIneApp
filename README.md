# FlightAnalytics — Streamlit Deployment

## Repo structure

```
├── app.py                  ← Streamlit app
├── requirements.txt        ← Python dependencies
├── deployment/
│   ├── preprocessor.joblib ← Fitted ColumnTransformer from Week 3
│   ├── xgb_model.joblib    ← Fitted XGBoost model from Week 3
│   └── artefacts.json      ← Feature names and metadata
```
